cd back
npm run start

cd front
yarn start

About database, you must create database that called maxdb and collection that called contacts in local


frontend: react
backend: nodejs

I thought It can conclude call values by pages, CRUD by mongoose，set CORS, UI by antd, POST/GET by Axios


Acually，I am messy, much work for me, although I only finished few parts, 
but I already have solutions for the rest, if I can have more time, I can finish all 